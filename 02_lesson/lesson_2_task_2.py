# Функция для определения високосного года
def is_year_leap(year):
    # Год считается високосным, если он делится на 4 без остатка,
    # кроме случаев, когда он также делится на 100, но не делится на 400.
    if year % 4 == 0 and (year % 100 != 0 or year % 400 == 0):
        return True
    else:
        return False

# Тестируем функцию на выбранном году
test_year = 2024  # Выберите любой год для тестирования
result = is_year_leap(test_year)

# Вывод результата в консоль
print(f"Год {test_year}: {result}")